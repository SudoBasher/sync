#!/bin/bash

# in the future, ask what is the source and target servers
# for now, set the source as remote server and target as the redudant server

# setup rsync executable script to the local storage folder

# sync the local storage folder to the cloud

# add the cron job to the source server

# run the script on the source server
